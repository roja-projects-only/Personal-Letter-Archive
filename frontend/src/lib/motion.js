/**
 * Shared motion constants — keeps stagger timing consistent across all list renderers.
 * Must stay in sync with --motion-stagger-step in index.css.
 */

/** Delay increment (ms) between successive staggered list items. */
export const LIST_STAGGER_MS = 60
